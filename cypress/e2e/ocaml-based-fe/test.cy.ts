

describe('TEST TEST', () => {
  beforeEach(() => {
    expect(true).to.equal(!false);
    // cy.visit(Cypress.config().baseUrl + '/dashboard');
  });

  it('goes to earliest block', () => {
    expect(true).to.equal(!false);
  });

});
